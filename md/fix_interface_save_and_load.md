# Fix bug ในหน้า interface ดังนี้
    # Bug  ที่เกิด
        1. Load ไม่ได้ load เนื้อหาจากไฟล์ที่เลือก
# สิ่งที่ต้องการให้ทำ
        1. แก้ Bug การ Load โดย ให้ load เนื้อหาจาก file ที่ save ไว้
        2. ปุุ่ม save as ให้ ถามก่อนว่า save file ชื่ออะไร
        3. หาก user กด save ครั้งแรก ให้ถาม user ว่าจะ save ชื่ออะไรก่อน
        4. เพิ่มปุ่ม export ให้ user สามารถ downloadเ file เข้าเครื่อง local ได้
        5. เพิ่มปุ่ม import ที่รองรับ file .txt และ .docx,.doc ให้ user upload file มาลงที่ editor ได้